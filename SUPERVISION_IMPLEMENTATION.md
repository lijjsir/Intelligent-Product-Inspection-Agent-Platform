# 质监业务第一版：运行、权限与接口说明

> 业务闭环的目标架构、四 Agent 交接协议、实施阶段和完成门禁，以
> [《质监系统四 Agent 闭环架构与实施计划 v3》](docs/质监系统四Agent闭环架构与实施计划_v3.md) 为准。
> 可直接领取的迁移、接口、算法、页面、测试和发布任务，以
> [《质监系统完整项目具体实施计划 v3.1》](docs/质监系统完整项目具体实施计划_v3.1.md) 为准。
> 本文件主要描述当前已存在的运行、权限和 API 底座；其中“可调用”不等于“四模块闭环已完成”。

> 命名纠偏：项目四个业务 Agent 固定为市场监控 Agent、舆情监测 Agent、监督抽查 Agent、实验室检测 Agent。
> 2026-09-24 已完成第一批 P0/P1 实施：前端菜单、Agent 配置和运行拓扑已统一原名；新增自适应检测 0102 迁移、目标/证据/决策契约和首版设备闭环页面。下述未完成项仍不得宣称完成。

对应申报书和项目汇报图的精简方案。首个验证场景为电动自行车。实现保留六类账号，以内部业务人员作为普通用户；数据按组织隔离，地区用于筛选和统计。四个业务 Agent 固定为市场监控、舆情监测、监督抽查和实验室检测，风险评级、标准核验、证据约束、可信复核、QDL 与记忆治理作为共用能力。

## 启用与运行

1. 安装后端依赖；本版新增 `openpyxl` 支持 XLSX，开发测试使用 `aiosqlite`。
2. 在实际 MySQL 环境执行 `alembic upgrade head`，新迁移为 `0100_quality_supervision`。它新增八张组织内业务表，扩展 SKU 的 `supervision_data`、批次数量与标准 `applicability`，不补造历史企业、地区或设备信息。
3. 启动现有后端、前端、Celery worker 和 beat。质监运行通过持久化队列记录执行；beat 每 30 秒派发漏发/超时运行并同步签发后的知识候选，未接入 worker 时页面显示等待处理。
4. 管理员打开监管对象或设备资源页面，为当前组织启用质监业务。既有图片任务保留原有路径；启用后新建图片任务生成分析草稿，测量和混合任务先进入采集中。
5. 管理员登记监管对象、产品、设备与标准。设备必须明确登记可检测项目和校准状态；标准需要生效条件、条款版本和可定位原文。地区字典作为内部配置使用，不占主菜单。
6. 专家处理业务复核；提交人与复核人必须不同。抽查确认和正式结果确认保留审计记录，不再要求管理员逐人配置两套业务授权。

标准与实际测量阈值应由业务专家核验。没有真实模型、测量、正常基线或适用标准时，系统输出待补证或人工复核；不生成失效概率，不把模拟流程当作真实业务验收。

## 角色与页面

| 角色 | 页面与操作 | 限制 |
|---|---|---|
| 管理员 | 组织与人员、监管对象、产品与批次、设备资源、检测标准、治理与日志 | 只维护组织内资料，不代替专家业务复核 |
| 普通用户／内部业务人员 | 舆情登记、市场监控查看、监督抽查查看、抽样确认、测量导入、补证和结果反馈 | 编辑创建或被指派的未确认记录；不处理本人提交的复核待办 |
| 专家 | 市场风险复核、舆情线索复核、监督抽查方案、检测结果复核 | 不复核本人编写或提交的同一版本 |
| 平台运营 | 分析中心、设备运行/校准/容量、业务记录查看、Agent 运行设置查看 | 新质监任务只读，不能批准、签发或更改已签发结果 |
| 算法工程师 | 正常响应基线、已授权业务样本、数据接入/评测/模型管理、概率校准评测 | 不直接读取新质监案件和任务；接收明确授权到本人数据集的候选样本 |
| 应用开发者 | Agent、路由与工具、设备连接、四类 Agent 的启停受理/超时/轮次设置 | 不获得业务判断和批准权限；设备连接凭据与公共档案分开保存 |

目标质量监督页面为：质监工作台、市场监控、舆情监测、监督抽查和实验室检测。“质监态势”作为市场监控报告页签，不作为 Agent 名称。平台通用的“任务管理”和“检测结果”不从属于实验室检测，合并在独立的“任务与结果”分组；实验室检测只管理样品、设备、实时测量、证据补足、下一项检测和停止决定。反馈从案件或结果上下文进入，地区与样品保留为内部数据或业务步骤。

后续页面迁移：产品主数据保留品牌、型号、企业、地区和数量；标准保留适用时间和地区；任务支持图片、测量和混合输入；实验室检测增加设备事件、证据状态、下一项检测与停止决定；结果保留未校准标记和确认锁定；业务待办位于质监工作台；Agent 管理只展示四个申报口径的业务 Agent。

会议室保留为人工会商入口，业务案件无需会议室。旧记忆图中的专业 Agent 占位节点已经删除，业务 Agent 只从统一质监入口运行。资料采用停用或归档，正式结果不能通过历史复核或任务删除接口覆盖。

## 四个 Agent 与协作

| Agent | 输入 | 产出 |
|---|---|---|
| 市场监控 Agent | 市场、企业、产品、地区、抽查覆盖、处置反馈与统计基数 | 时间窗变化、风险聚集、来源分布和抽查覆盖提示；不将案件增长冒充产品失效率 |
| 舆情监测 Agent | 投诉、舆情、图像、产品企业与标准快照 | 实体与风险线索归一、情绪和事实分离、证据引用、冲突及缺失项 |
| 监督抽查 Agent | 已确认案件、类别覆盖、预算、工时、设备能力与容量 | 可复核的监督抽查对象、项目、成本、未纳入原因；不宣称全局最优 |
| 实验室检测 Agent | 实物样品、必检项/候选项、设备能力与校准、实时测量、标准和正常基线 | 产品/设备异常区分、证据状态、下一项检测、继续/补测/停止建议和结果草稿 |

流程：市场监控与舆情监测并行形成两个版本化输入 → 共用风险评级与专家复核 → 监督抽查方案 → 另一位专家确认 → 系统生成任务、样品和检测目标 → 实验室检测 Agent 在设备实时反馈下循环补足证据 → 达到停止条件或转人工/完整检测 → 专家结果复核与正式确认 → 反馈和知识候选。

四类业务运行均通过 `AgentManager → ManagerLoop → ManagerDispatcher` 正式入口执行，并以 `BusinessAgentEnvelope` 交接对象版本、证据、知识快照、业务状态和下一步动作。旧的 `run_supervision()` 旁路已删除；证据不足等业务状态不会被“函数返回成功”覆盖为正式完成。

批准后生成的样品仅为待抽样编号，不能计为已经完成抽样。业务人员填写真实抽样时间后才可接收该样品测量。

任务版本、运行输入和知识来源分别保存；分析失败、缺证、人工等待、取消、来源过期分开表示。并行专业步骤使用独立状态和数据库会话，按依赖安全汇合；质检终判等待视觉、证据和实验室步骤。

关键记录保存在 MySQL；Redis 黑板用于协调，不替代长期事实源。分析启动前提交运行检查点，失败/中断最多按配置恢复两轮；技术人员可暂停新受理并配置 30–600 秒超时。变更或撤回来源后，按实际消费版本标记受影响记录，未消费该版本的分支保持原状。

## 与 v3 目标架构的差距（2026-09-22）

以下只记录当前实现事实，不表示已经开始本轮代码迁移：

1. 四个 Agent 原名已进入菜单、配置和运行拓扑；历史 API 操作名仍需通过兼容层逐步整理，不能直接删除旧数据中的 Agent ID。
2. 市场监控目前仍以确定性数量、分布和变化统计为主；已冻结正式报告 kind，但尚未完成独立版本化 `MarketMonitoringReport` 的创建、复核和回测链。
3. 舆情报告、市场报告和 `RiskAssessment` 契约已经定义，两个 Agent 输出的自动汇合、独立复核和下游依赖仍未完成。
4. 抽查计划和检测会话已经区分 `required_items` 与 `candidate_items`；CP-SAT 求解、版本化停止策略和计划审批物化仍需实施。
5. 实验室检测已支持 `InspectionGoal`、不可覆盖 `EvidenceState`、下一项检测建议、强门禁停止建议及另一位专家审批；结果草稿关联和跨重启工作流仍未完成。
6. 设备测量接入现在保存命令关联、序号、校准版本和验证状态，并能更新双目标 EvidenceState；设备命令申请、批准、下发和回执 API 尚未实现。
7. 前端已展示设备可信状态、产品质量状态、证据充分度、下一检测、停止评估和专家停止审批；设备事件时间线尚未完成。
8. 当前自动化验证只能证明工程门禁，不能证明真实检测项目减免或周期压缩；仍需真实设备、标准、样品和同口径对照试验。

上述差距按 v3 的 P0–P5 顺序实施。在名称、接口和停止门禁完成设计评审前，不应继续扩展页面或对外宣称自适应检测闭环已经完成。

## 数据字典与来源

新业务档案采用 `supervision_records` 保存，按 `org_id + kind + code` 唯一，具体 `data` 使用严格 Pydantic 模型验证。`supervision_revisions` 保存每次输入/分析版本，`supervision_runs` 保存冻结输入与输出，`business_reviews` 保存针对具体版本的复核与确认；另有连接凭据、接收批次、测量和依赖表。

| 类型 | 必要信息 |
|---|---|
| 企业 | 编号、名称；信用代码、企业角色、地址、地区、产品关联可补齐 |
| 地区 | 编码、名称、父级与字典版本；导入支持父级编码并检查循环 |
| SKU／批次 | 既有产品/批次标识，品牌、型号、企业、生产地、销售地、生产日期与数量 |
| 案件 | 编号、名称、描述、发生时间；产品、企业、批次、四类地理关系、标准、负责人、来源证据 |
| 证据 | evidence_id、source_type、source_id、occurred_at、text；附件、哈希、observed/inferred/synthetic 性质 |
| 设备 | 编号、类型、位置、能力、校准/有效期、在线状态、容量；专用连接身份单独存储 |
| 样品 | 编号、任务、产品、批次、实际抽样时间、抽样地 |
| 会话 | 任务、样品与设备、检测项目及方法单位、标准限值/引用、基线版本、关联案件/计划 |
| 暴露量 | 地区、类别、时间区间、销量/在用量/实际抽查数、来源编号 |
| 反馈 | 目标版本、确认/误报/漏报/不确定/新证据、发生时间与处理人 |

生产地、销售地、投诉发生地、抽样地分别记录。地区无辖区授权含义；组织隔离是第一版访问边界。

QDL v2 保留旧会议来源，同时支持风险案件、任务、标准、设备和检测会话来源；没有会议来源时必须填写 `source_context`。签发后通过现有 MemoryService 的候选门禁发布，禁用自动晋升，候选保留可追溯 QDL，索引仍沿用现有治理/outbox。普通模型草稿不产生可共享质量记忆。

## API 契约

所有路径均以 `/api/v1` 为前缀，保留现有响应封装。业务账号使用 Bearer token，服务端每次读取实际角色、启停与授权；设备使用专用 `X-Device-Token`。

| 接口 | 行为 |
|---|---|
| GET/PATCH `/quality-supervision/settings` | 查询状态；管理员按组织启用 |
| GET/PATCH `/quality-supervision/agent-config` | 查看运行设置；管理员/开发者修改四 Agent 的 enabled、timeout_seconds、max_rounds |
| GET/POST `/{kind}`，GET/PATCH `/{kind}/{id}` | 企业、地区、设备、案件、计划、会话、样品、暴露量、基线；创建 `{code,name,data}`，更新 `{version,name?,data?,status?}` |
| GET `/{kind}/{id}/revisions` | 不可覆盖的历史输入/分析版本 |
| POST `/regions/imports/preview`、`/risk-cases/imports/preview` | multipart 文件与列名映射，返回错误行、有效数与 preview_id |
| POST `/{kind}/imports/confirm?preview_id=…` | 确认该账号已预览的文件，重复确认不重复创建 |
| POST `/{kind}/{id}/runs` | `{version,request_key,operation}`；public_opinion/market_monitor/sampling/laboratory 分别调用四个业务 Agent |
| GET `/supervision-runs/{id}`，POST `/{id}/retry`、`/{id}/cancel` | 查看进度、受控失败重试、发起人取消；取消和旧版本输出不能落入当前记录 |
| POST `/risk-cases/{id}/manual-assessment` | 专家 `{version,risk_level,evidence_ids,reason}`；真实引用与标准条件仍需完整，由其他专家复核 |
| POST `/risk-cases/{id}/reassessment` | 新建关联重评估记录，保留旧记录 |
| POST `/{kind}/{id}/reviews` | `{version,operation}`，支持 risk.review、sampling.approve、result.review、result.signoff |
| GET `/business-reviews`，POST `/{id}/decision` | 按人员权限汇总待办；处理 `{decision,comment}`，accept/revise/request_evidence/escalate；拒绝自批准及旧版本批准 |
| POST `/devices/{id}/connection` | 管理员/开发者轮换，只显示一次新凭据，旧凭据失效；档案与日志不含明文凭据 |
| PATCH `/devices/{id}/runtime` | 管理员/运营维护在线、校准、有效期与容量；影响消费版本时触发重评估 |
| POST `/device-ingest/measurements` | 设备数据接收；绑定设备/组织校验，按批次与事件去重，同编号不同内容返回冲突 |
| GET/POST `/inspection-sessions/{id}/measurements` | 查测量；业务人员可提交同结构测量数据 |
| POST `/inspection-sessions/{id}/imports/preview` | CSV/XLSX 检查，保留原文件、映射和错误行 |
| POST `/inspection-sessions/{id}/imports/confirm` | preview_id 与 source_key；会话版本变化后须重新预览 |
| GET `/inspection-sessions/{id}/pending-measurements` | 查看单位/项目等尚未匹配的原始批次；这些批次不计入完整度 |
| GET `/quality-supervision/measurement-template` | UTF-8 CSV 模板 |
| GET `/quality-supervision/analytics` | 固定时间窗、地区/类别分布、计划与检测覆盖；不冒充真实失效率或响应压缩成绩 |
| POST `/quality-supervision/evaluations/calibration` | 算法工程师提交组织内评测集编号、配对概率与真实标签，返回 Brier/ECE 与可靠性区间 |
| POST `/inspection-sessions/{id}/dataset-enrollments` | 业务人员明确授权有效签发版本到指定组织内数据集 |
| GET `/quality-supervision/dataset-enrollments` | 算法工程师只查看授权到本人数据集的来源；通过已有任务 ingest 接口导入候选 |

测量请求示例（标识替换为实际登记 UUID）：

```json
{
  "session_id": "<检测会话UUID>",
  "source_key": "device-batch-20260916-001",
  "measurements": [{
    "event_id": "reading-001",
    "sample_id": "<样品UUID>",
    "device_id": "<设备UUID>",
    "item": "温升",
    "measured_at": "2026-09-16T10:00:00+08:00",
    "value": 12.4,
    "unit": "C",
    "method": "<登记的检测方法>",
    "quality_flag": "valid"
  }]
}
```

检测项目必须匹配计划和设备能力。时间带偏移时转为 UTC，未带偏移的接口时间按 UTC；页面按本地时间选择。数值禁止 NaN/Infinity。文件每批 1–5000 行、最大 10MB；`preview:` 来源前缀由系统保留。错误原始批次保留待处理，修正后用新的来源批次编号重新提交。

签发结果的数据库兼容分值为 0，响应 `score_status=not_calibrated`，页面显示未校准，不能解释为 0% 可信；风险概率始终未知，直到完成独立校准并另行发布模型。数值检测异常保存在可追溯 findings 中，不伪造图像框或模型置信度。

## 测试与验收

- 后端全量回归及 `tests/test_supervision.py`：角色/组织隔离、自复核拒绝、版本、字典循环、CSV/XLSX、事件重传与冲突、设备能力与校准、实际抽样、独立复核、结果锁定、恢复与幂等、并行状态隔离、数据集授权、校准公式、迁移升级与回退。
- 前端：类型、lint、构建、单测；菜单权限与可编辑输入边界。
- `frontend/tests/supervision_e2e.py`：真实 HTTP 与页面，无浏览器接口模拟；使用隔离 SQLite 与测试材料，贯通电动自行车线索 → 人工/模型分支 → 抽查批准 → 实际抽样登记 → HTTP 测量 → 复核签发，并检查桌面/移动页面。
- 隔离 QA 通过只能证明业务流程和接口，不能替代生产 MySQL、真实模型、Embedding、设备与现场业务效果验收。

运行常规检查：后端 `pytest tests -q`；前端 `npm run typecheck`、`npm run lint`、`npm run test -- --run`、`npm run build`。

2026-09-16 验证记录：后端全量 1084 项通过；变更后的相关专项 43 项通过；前端 99 项通过，类型检查、lint、构建通过；隔离 API+UI 电动自行车场景通过。隔离测试有生产路由、服务和实际 HTTP/页面操作，材料与数据库为测试数据，未运行生产 MySQL 升级或真实设备/模型效果验收。

隔离场景复现：在已安装开发依赖的环境启动 `python backend/scripts/supervision_qa_server.py --isolated-test-data`（仅专用 SQLite 测试库，端口 8123），将前端代理指向 `http://127.0.0.1:8123`，启动前端 5174，然后运行 `python frontend/tests/supervision_e2e.py`。`SUPERVISION_QA_API`、`SUPERVISION_QA_WEB`、`SUPERVISION_QA_ARTIFACTS` 可覆盖地址和产物目录。QA 服务生成临时测试账号，不能部署到公共网络或作为业务验收环境。

## 研究指标与供应条件

| 指标/工作包 | 本版能力 | 后续必须取得的证据 |
|---|---|---|
| 四个业务 Agent | 可执行定义、统一入口、版本化产出与运行配置 | 各业务场景的真实数据与示范验收，成果名称与申报计数对齐 |
| 季均查验类别 >20 | 有类别、计划、样品和签发任务基础 | 固定类别字典及真实季度查验台账，不能用演示配置充数 |
| 响应压缩 >50% | 有运行与签发时间；提供同口径对照计算工具 | 相同有效受理至结果确认起止点的基线与真实案件，中位数/P90/样本量 |
| 设备周期优化 | 提前预警、完整度、仪器/产品异常分离、补测与实际结果 | 真实设备时序、正常基线和完整检测真值；不能用预警时间替代正式完成时间 |
| 校准与模型优化 | Brier/ECE 工具、基线/授权样本和既有训练评测入口 | 独立时间/企业/批次划分的真实标签、消融/回测、模型与提示版本发布 |
| 知识一致性与恢复 | 来源版本依赖、受影响分支标记、新重评估记录、候选准入 | 真实冲突、撤回、局部恢复与误伤验证；重评估仍需业务复核 |
| 机理、探针、LoRA、长尾增强 | 保留既有算法工作台入口，本版不声称完成科研模型 | 模型内部访问、研究数据、训练和保真性/稳定性实验报告 |

真实数据供应、正式标准、设备适配、模型与 Embedding 服务、科研模型内部访问、专家责任和人工效率基线由试点环境提供。缺少这些条件时保持待处理状态，并分别记录工程测试与真实验收。
